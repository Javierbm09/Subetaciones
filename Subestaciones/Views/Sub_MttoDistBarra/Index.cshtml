@model IEnumerable<Subestaciones.Models.Sub_MttoDistBarra>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstadoBarra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conexiones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoPuentes)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoBarra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Conexiones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoPuentes)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
